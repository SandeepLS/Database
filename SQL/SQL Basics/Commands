1)Create database:- CREATE DATABASE databasename;

2)Create table:-
create table Customers(
CustomerID int primary key,
CustomerName varchar(225),
CustomerLN varchar(22),
City varchar(30),
Country varchar(30),
PostalCode varchar(30)
);

3)Insert table Values;- INSERT INTO Customers VALUES (101, 'Sandeep', 'LS', 'Bangalore', 'India', 560043);

4)Insert 1 extra column in a table:-
ALTER TABLE Customers
ADD Price varchar(255);

5) After the add new column in table, the we add the column values:-
UPDATE Customers
SET Price = '77'
WHERE CustomerID=106;



